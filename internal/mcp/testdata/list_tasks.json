{
  "properties": {
    "project": {
      "description": "project slug filter",
      "type": "string"
    },
    "schema_version": {
      "const": 1,
      "description": "tool input schema version",
      "type": "integer"
    },
    "status": {
      "description": "status filter",
      "type": "string"
    }
  },
  "type": "object",
  "required": [
    "schema_version"
  ]
}
