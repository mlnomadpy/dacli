{
  "properties": {
    "apply_safe": {
      "description": "exact plan id returned by a prior dry-run",
      "type": "string"
    },
    "dry_run": {
      "description": "produce the immutable cleanup plan without writing",
      "type": "boolean"
    },
    "project": {
      "description": "project slug",
      "type": "string"
    },
    "schema_version": {
      "const": 1,
      "description": "tool input schema version",
      "type": "integer"
    }
  },
  "required": [
    "schema_version",
    "project"
  ],
  "type": "object"
}
