# Build output
/dacli
/dist/

# Go
*.test
*.out

# Editors
.DS_Store
.idea/
*.swp

# The repo's own dacli workspace IS committed — dacli manages its own
# backlog (the ARCHITECTURE § 3 dogfood test). Transcripts and compiled
# output stay out via .dacli/.gitignore (runs/, build/), written by init.
