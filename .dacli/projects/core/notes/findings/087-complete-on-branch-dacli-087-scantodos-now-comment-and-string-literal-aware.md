---
id: f-087-complete-on-branch-dacli-087-scantodos-now-comment-and-string-literal-aware
kind: note
note_kind: finding
created: 2026-07-23T00:06:25Z
created_by: a-bndgc6d73j
about: [[087]]
severity: moderate
---
# 087 complete on branch dacli/087: scanTodos now comment- and string-literal-aware, self-scan free of false positives
Commit 2bf74a1 by a-bndgc6d73j (fixer), staged ONLY the 2 intended files (git add + dacli commit --no-add): internal/features/onboard/onboard.go, internal/features/onboard/onboard_test.go (new). All 4 acceptance criteria met: (1) scanTodos (onboard.go) now finds the comment portion of each line via commentText(), a small tokenizer that tracks quote state (single/double/backtick, with escape handling) so a comment-start sequence appearing only inside a string literal (Go's []string{"TODO",...}, or a test fixture whose string payload literally contains "// TODO") is never mistaken for a real comment; within that comment text the marker must LEAD it (word-boundary via isWordByte) — this is what actually filters out onboard.go's own doc-comment prose mentioning 'the TODO markers' and a header comment that literally began 'TODO markers - cheap scan...' (reworded to 'Scan for TODO/FIXME/HACK/XXX markers...' since it was prose about the feature, not an actionable item) — see decision note for why lead-of-comment (not just standalone-token-anywhere) was required. onboard.go:249's []string{...} literal, gates.go:465's []string{...} literal, the command Brief at onboard.go:30, and onboard_test.go:52/55's assertion strings all have no comment token on their line at all, so they were already excluded before the quote-tracking even engages. (2) New internal/features/onboard/onboard_test.go:TestScanTodosIgnoresStringLiterals builds a fixture with a real '// TODO: handle x' comment, a 's := "TODO in a string"' line, and a 'markers := []string{"TODO", "FIXME"}' line, and asserts exactly 1 marker found with marker=TODO, loc=fixture.go:3, text='handle x'. (3) Verified via a throwaway test (written, run, then git-clean'd — not part of the diff) that walk() over this repo's own tree now returns 0 todos, vs. 13 self-referential false positives before the fix; internal/cli.TestAdoptExistingRepo (which needs the real pay.go // TODO: handle the batch path marker to still be found and seeded under --todos) still passes. (4) go build ./... exit 0; go test -exec 'env -u DACLI_AGENT' ./internal/... all green (DACLI_AGENT stripped because this session itself runs as a dacli agent and the var otherwise leaks into in-process CLI test commands — pre-existing test-isolation gap noted by other agents, not from this task). Owner: verify and close via dacli task check/done + dacli merge --task 087.
