---
id: f-183-complete-on-branch-dacli-183-commit-b0116e0-site-gitignored-node-modules
kind: note
note_kind: finding
created: 2026-08-04T12:15:30Z
created_by: a-maintainer-y2t0se
about: "[[183]]"
severity: moderate
---
# 183 complete on branch dacli/183-... (commit b0116e0): site gitignored + node_modules excluded from go build
Branch dacli/183-gitignore-site-and-exclude-node-modules-from-the-go-build, commit b0116e0 by a-maintainer-y2t0se. Staged ONLY 3 files: internal/features/dashboard/ui/package.json (+1: postinstall hook), internal/features/dashboard/ui/scripts/exclude-node-modules-from-go.mjs (new), internal/features/dashboard/nodemodules_test.go (new).

ACCEPTANCE:
(1) site is gitignored — ALREADY satisfied by .gitignore ('site/' and '/site/'); verified 'git check-ignore site/ site/index.html' matches. No change needed.
(2) go build/test/vet do not traverse node_modules — FIXED. Verified on go1.26 that 'go build ./...' DOES descend into node_modules: a stray *.go there breaks the build (repro: go list ./... listed a planted node_modules pkg; go build errored on a broken vendored.go). This is NOT local-dev-only as the prior deferral note (r-183-part-a-...) claimed — ci.yml runs 'npm ci'+build then 'go vet ./...'/'go test ./...' with node_modules PRESENT. Fix: an npm postinstall drops a lone go.mod into node_modules, a nested-module boundary the parent module's ./... walk skips. Confirmed on the real tree: with the marker present, 'go build ./...' passes despite a broken .go planted under node_modules; without it, the build fails.

WHY not the standard nested ui/go.mod: it breaks '//go:embed all:ui/dist' in dashboard.go ('cannot embed directory: in different module' — reproduced), matching the earlier deferral. Moving dist out of ui/ would fix that but rewrites the documented dist/embed/goreleaser-clean-tree convention. The node_modules marker is regenerated by postinstall on every install/ci and, being under gitignored node_modules, never dirties the tree (goreleaser clean-tree check + the embed both unaffected).

PROOF: gofmt -l internal/ clean; go build ./... clean; go vet ./... clean; go test ./... green EXCEPT TestCatalogRefusesRatherThanWritingAnEmptyRoster (features/catalog), which is the known DACLI_AGENT env-leak test-isolation gap (this session runs as a dacli agent) — passes with 'go test -exec env -u DACLI_AGENT ./internal/features/catalog/', unrelated to this change. New tests: TestNodeModulesExclusionWired (guards the postinstall wiring) and TestNestedGoModExcludesNodeModules (guards the go.mod-boundary mechanism) both pass.

Owner: 'dacli accept 183' then 'dacli merge --task 183' (PR-first off for this run).
