---
id: f-089-complete-on-branch-dacli-089-skill-promote-implemented-owner-gated-tested
kind: note
note_kind: finding
created: 2026-07-23T09:49:35Z
created_by: a-sjnmw4x20p
about: [[089]]
severity: moderate
---
# 089 complete on branch dacli/089 — skill promote implemented, owner-gated, tested
Commit 5aa70e5 by a-sjnmw4x20p (fixer), staged only the 4 intended files (git add + dacli commit --no-add): internal/features/skillforge/skillforge.go, internal/store/lessons.go, internal/cli/skills_test.go, docs/SKILLS.md. Replaces the clikit.Planned stub at skillforge.go:26. Design: a lesson = any scope:workspace note of kind decision/finding/ref (store.lessonKinds, same set store.WorkspaceLessons already surfaces cross-project). Added store.FindLesson(w, ref) resolving a lesson anywhere in the workspace by note id or level-1 title, refactored the doc->Lesson conversion (lessonFromDoc) shared with WorkspaceLessons, and added a Lesson.Origin field so provenance rides along. cmdPromote in skillforge.go gates on id.ID == agentid.RootID specifically (not merely CanMutate/grant rw — see the paired decision note skill-promote-gates-on-identity-a-root-not-merely-grant-rw), refusing every spawned agent regardless of grant (exit 3), per SKILLS.md § 6's 'human on the gate' requirement against the hostile-file -> finding -> lesson -> auto-compiled-instructions escalation. On success it writes a versioned skill.md (version: v1, like skill add) under w.SkillsLibDir()/<name>/ with description=lesson title, promoted_from=lesson id, origin inherited from the lesson if present, and body from the lesson; refuses (exit 1) rather than clobbering an existing skill of the same name; an unresolvable ref is store.ErrNotFound (exit 4). Test: TestSkillPromoteOwnerGated in internal/cli/skills_test.go covers all 4 paths (project-scoped note not promotable/not-found, spawned rw agent refused, root promotes successfully with origin+version+promoted_from verified in the written file and via skill list, re-promote same name refused, unknown ref not-found). go build ./... clean; go test ./internal/... all green. KNOWN GAP (documented in SKILLS.md § 8 item 4, not in this task's acceptance): dacli taint does not yet walk the skills library, so a promoted skill's inherited origin: field is not yet reachable by a taint walk — store.Taint only walks events and project notes dirs today. Filed as an open question, not a bug in this task's scope.
