---
id: f-066-g5-complete-on-branch-dacli-066-commit-0901532-all-3-acceptance-met
kind: note
note_kind: finding
created: 2026-07-22T18:29:16Z
created_by: a-n03m4hw62x
about: [[066]]
severity: major
---
# 066 G5 complete on branch dacli/066-...; commit 0901532; all 3 acceptance met
Committed 0901532 by a-n03m4hw62x (maintainer), staged ONLY internal/features/ghmirror/{ghmirror.go,ghmirror_test.go} (git add + dacli commit --no-add --force; --force noted the brief-authorized test file, outside the ghmirror.go-only recorded claim — see decision note). ACCEPTANCE all satisfied, reusing the existing marker/searchByMarker/write-back machinery (gate + idempotency NOT reinvented): (1) new --findings-as-issues flag on 'dacli github push <project>' — for every finding note (store.ListNotes via findingNotes, which reads notes WITH on-disk paths like decisionNotes) it opens ONE issue: title=finding title, body=findingIssueBody(marker + '**Severity:**' + finding detail via findingText + note-id backlink), labels 'finding' + severityLabel(sev)=severity:<major|moderate|minor> (empty/unknown -> severity:unspecified, total mapping) created best-effort via ensureLabel. Idempotent: findingIssueMarker keyed on note id + ws id with a DISTINCT prefix (<!-- dacli-finding-issue: -->) from task/decision/finding-comment markers; mappedIssueDoc(frontmatter) first, then searchByMarker (strong-consistency list endpoint substring), only then create; the issue number is written back onto the finding note's github: frontmatter block (mdstore.WriteFile) so a re-push NEVER duplicates. (2) DISTINCT from G4: default (no-flag) push keeps mirrorFindings (finding COMMENTS on the task issue) intact; --findings-as-issues skips that path and instead mirrors standalone issues once after the task loop; honors the SAME disclosureGate already tripped in cmdPush (linked repo + PUBLIC consent + live visibility re-check), operator-triggered only, never on ship. (3) go build ./... clean; go test ./internal/... green (incl. internal/cli TestFeatureSlicesAreIsolated, internal/features/ghmirror; the one transient TestVerdictReviewRendersRecordedVerdicts flake in internal/features/vcs is a pre-existing same-millisecond ULID ordering race, unrelated — passes on rerun, count=5 green); gofmt+vet clean. Unit tests added on fixtures, NO live gh: TestSeverityLabelMapping, TestFindingIssueMarkerKeying (no cross-marker substring collision), TestFindingIssueBody, TestFindingIssueMappingIdempotency (write-back -> mappedIssueDoc), TestFindingNotesEmpty. Refactor: extracted generic noteFiles(w,project,kind); decisionNotes/findingNotes are thin wrappers; decisionNote struct renamed noteFile (G2 tests unaffected). Box-checking refused for non-owner (only a-root) — owner: verify and close via dacli task check 066 --n 1..3 + task done 066, then dacli merge --task 066.
