---
id: f-114-complete-on-branch-dacli-114-commit-5c1bbdc-pr-267-opened-with-auto-queued
kind: note
note_kind: finding
created: 2026-07-26T21:53:10Z
created_by: a-hp40s2ycrb
about: [[114]]
severity: major
---
# 114 complete on branch dacli/114-... commit 5c1bbdc, PR #267 opened with --auto queued
Root cause confirmed: orchestration.go's self-PR LAND step (was line 441) ran ship --no-accept --no-integrate --push unconditionally every cycle, pushing a data-only .dacli record commit straight to main while a same-cycle fixer's dacli pr --auto (lifecycle.go cmdPR) still had its auto-merge queued and CI pending -- under strict branch protection GitHub reads that push as the PR falling behind and never merges it, stalling even at --width 1 (issue #75). Fix: added driver.pendingLand ([]string of self-PR branches opened this run) and driver.recordSelfPR/stillPending (orchestration.go). Each cycle, before shipping the record, stillPending does a best-effort  then drops any branch whose refs/remotes/origin/<branch> is gone -- both dacli pr --auto and dacli integrate --pr always pass --delete-branch (lifecycle.go:213,758,794), so a vanished remote-tracking ref is a precise, dependency-free signal the PR actually landed (or was closed), mirroring the same pattern trunkMarker/branchExists already use in this file. ship still runs --no-accept --no-integrate every cycle (so tasks close/local record commits accumulate normally); --push is appended ONLY when pendingLand is empty, so main never advances while any self-PR from this run is still in flight, and the held-back record commits catch up in one push the first cycle nothing is pending. Also fixed: the record ship's (out, err) was previously discarded entirely (silent failures); now a failed ship logs a line. New regression test TestRecordSelfPRHoldsPushWhileBranchPending (driver_test.go) uses a real bare-repo origin + pushed branch to prove --push is withheld while the branch is still on origin and resumes once it's deleted (simulating the merge). go build ./... clean; go test ./internal/... all green (incl. orchestration, ship). Filed a separate finding (loop's local main checkout never syncs with origin after an async gh pr merge --auto lands) documenting a related but out-of-scope latent divergence bug this fix's success will start exposing -- worth its own follow-up task. Owner (a-root): verify and close via task check/done + merge --task 114 once PR #267 (auto-merge queued) lands.
