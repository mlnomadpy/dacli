---
id: f-122-complete-on-branch-dacli-122-dashboard-command-http-handler-and-handler
kind: note
note_kind: finding
created: 2026-07-23T19:25:07Z
created_by: a-qr6b08292c
about: [[122]]
severity: moderate
---
# 122 complete on branch dacli/122: dashboard command, HTTP handler, and handler tests
Committed ad68c25 + 3451d61 on branch dacli/122-dacli-dashboard-local-web-ui-to-monitor-the-project-and-the-live-agent-swarm by a-qr6b08292c (fixer), via git add + dacli commit --no-add so only the intended files landed: internal/features/dashboard/{dashboard.go,dashboard_test.go,static/index.html} and internal/cli/cli.go (new import + aggregate entry). ACCEPTANCE: (1) 'dacli dashboard' (internal/features/dashboard/dashboard.go cmdDashboard) binds 127.0.0.1 (an ephemeral port unless --port pins one) and serves a single embedded page (go:embed static/index.html, self-contained: no CDN/external JS/CSS) showing projects with per-status task counts + burndown (done/remaining points, per-day completions, mirroring insight.cmdBurndown's math) and the live agent swarm (task/role/runtime/pid/uptime/last-activity), plus the pending (unsynced) event-log count that dacli status already surfaces — read via store.ListProjects/ListTasks, a duplicated liveAgents() over procmon.ReadRecord+AliveRecord (same pattern as execution.liveAgents; duplicated rather than imported because feature slices never import each other, enforced by internal/cli/arch_test.go), and eventlog.List(Pending:true). (2) The page's JS polls GET /api/state every 2s (see static/index.html poll()), so a running loop's agents appear live without restarting the server; no other network dependency. HANDLER TEST: internal/features/dashboard/dashboard_test.go drives newHandler(w) through httptest (no real port bound): TestIndexServesEmbeddedPage, TestUnknownPathIs404, TestAPIStateReportsProjectsAndLiveAgent (asserts project counts/burndown/agent fields/pending_events against a workspace seeded with a real live process — this test's own PID, so procmon.AliveRecord genuinely probes a live PID/start-time pair), TestAPIStateOmitsDeadAgent (PID 0 must not appear live). go build ./... clean; go test ./internal/... all green. NOTE: could not smoke-test the compiled binary end-to-end in this session — running an arbitrary local binary requires interactive approval that a headless agent session cannot answer, so verification stopped at the httptest layer (which does exercise the real net/http.Handler, just not a real TCP listener). Owner: verify (a manual 'dacli dashboard' + browser check is cheap) and close via dacli task check/done + dacli merge --task 122.
